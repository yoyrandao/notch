apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-mock.fullname" . }}
  labels:
    {{- include "helm-chart-mock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-mock.selectorLabels" . | nindent 4 }}
